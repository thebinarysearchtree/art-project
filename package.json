{
  "name": "art-project",
  "version": "0.1.0",
  "description": "An art project.",
  "author": "Andrew Jones",
  "type": "module",
  "scripts": {
    "server": "node server.js",
    "start": "npm run server",
    "build": "esbuild src/index.js --bundle --minify --loader:.css=text --outfile=build/index.js"
  },
  "devDependencies": {
    "express": "^4.17.3",
    "http-proxy-middleware": "2.0.3",
    "esbuild": "0.14.27"
  }
}
